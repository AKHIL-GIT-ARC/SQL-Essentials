# LTRIM & RTRIM

## What are LTRIM() and RTRIM()?

`LTRIM()` removes spaces from the beginning of a text value.

`RTRIM()` removes spaces from the end of a text value.

## Syntax

```sql
LTRIM(value)
RTRIM(value)